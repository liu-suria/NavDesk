# 模块接口收敛补充（本地未发布）

- 管理模块改为显式传入服务的工厂实例，移除 `window.navdeskClient`。
- 编辑模块由大量首页内部函数依赖，收敛到导航、置顶、图标、提示、管理入口五项服务。
- 导航读写统一，保存成功后才发布到页面；置顶存储与变更通知统一，首页和管理页复用同一规则。
- 登录续期显式接收请求方法，非首屏模块不再直接读取启动、图标全局对象。
- 首屏 gzip 从 13,697 B 到 13,809 B（+112 B）；首屏延迟模块请求仍为 0。
- 29 项测试通过；新增服务测试验证失败保存不发布、置顶存储失败不误报成功、多个实例读取同一存储。构建验证覆盖生成模块语法与依赖。
- 浏览器验证新增、编辑、置顶排序、URL 修改后的置顶迁移、登录失效继续保存、完整备份恢复、独立管理页置顶入口、390 px 无溢出和无模块预加载。演示数据及测试置顶已恢复。

以下为前一轮结构与性能记录，未重新测量本轮限速耗时。

---

# 首屏优先重构 · 当前验收（2026-09-23，本地未发布）

本轮检查了首页、独立管理页、构建、图标及 API 启动路径。保留仍在使用的管理入口和接口；只删除确认失效的选择器、旧排序分支及过期生成文件。

## 结构与清理

- 首页仅保留浏览、搜索、时钟、登录和轻量模块入口；编辑/菜单/排序 DOM 与实现移入 `editor.mjs`，点击后加载。
- 弹窗样式从首屏 CSS 拆至 `interactions.css`，所有管理入口共用一个按需样式模块。
- 删除内联 `recovery.js`，会话失效时才导入 `recovery.mjs`，保持页面草稿。
- 删除未使用的 `.arrow`、`.admin-link` 规则和已由分类管理替代的首页分类排序分支；管理员品牌规则归回管理页。
- 去除图标队列失效字段；初次无搜索词时不再重建网址标题文本。
- 构建集中生成全部哈希模块、替换引用、设置静态模块长期缓存，成功后清理过期产物；导航 API 继续 no-store。
- README 改为当前实际架构，移除多轮相互覆盖的过时操作说明。
- 回归修正新增网址重复检查把自身识别为重复的问题。

首页从 62,269 B / gzip 18,306 B 降到 44,753 B / gzip 13,697 B；压缩体积减少约 25%。无新增依赖。生成模块不能单独漏发，首页打开不下载这些模块。

## 实际回归

26 项 Node 测试通过，含首屏体积预算及生成模块依赖完整性检查。浏览器通过：按需新增/编辑/排序、登录续期后继续保存、管理备份恢复、万年历、401 不显示私有链接、503 后重试、独立管理页及其整理入口。测试新增数据已通过原快照恢复。

320/390/430/1365 px 页面无横向溢出；手机卡片 44 px，编辑模块请求为 0。未完成截图视觉对照，布局验证使用 DOM 几何与计算样式。图标时序专项检查：列表 146 ms 可用，首图标请求 181 ms 发起。

## 本地限速测量

原始结果：`REFACTOR_PERFORMANCE.json`。浏览器禁用缓存，模拟 4 Mbps 下载/150 ms 延迟，模拟 API 额外延迟 250 ms；本地 HTTP 未启用压缩。手机与电脑各三次：

| 视口 | 首次内容绘制 FCP | 列表可用 | 首屏导航请求 | 首屏延迟模块请求 |
| --- | --- | --- | --- | --- |
| 手机 390 px | 232–244 ms | 455–465 ms | 1 | 0 |
| 电脑 1365 px | 236–252 ms | 439–447 ms | 1 | 0 |

不能将本地模拟结果等同线上耗时。真实 EdgeOne 鉴权与 Blob 延迟仍影响列表；本轮没有发布或修改线上业务数据。以下保留较早验收作为历史，不代表当前实现。

---

# 首次打开链路验收 · 2026-09-23

## 范围

本记录为首页和管理页正式推送前的本地验收；线上部署状态需推送后单独核验。没有读取或写入线上 Blob，也没有修改鉴权、Cookie 或数据结构。所有业务交互验收使用本地模拟接口与演示数据。

原链路：HTML → 外部 CSS/JS → 数据请求 → 展示。管理页原先先查 session 再取数据。新链路：HTML 内联样式/交互 + 并行数据请求 → 鉴权成功后展示链接。静态外壳不等待接口。

首屏外部 JS/CSS：首页 2 → 0，管理页 3 → 0。管理页启动 API 请求 2 → 1；登录提交后的 API 请求 3 → 2。首页约 20.5 KB（gzip 约 7.1 KB），管理页约 33.6 KB（gzip 约 10.3 KB）。gzip 为本地计算，实际部署是否启用压缩需线上核验。

## 电脑和手机测量

本地 Chromium，禁用缓存，模拟 4 Mbps 下行、150 ms 网络延迟；模拟数据接口额外延迟 250 ms。每个视口测三次。

| 视口 | 页面交互就绪 | 私有导航内容就绪 | 数据请求 | 外部 JS/CSS |
| --- | --- | --- | --- | --- |
| 电脑 1365×900 | 199–203 ms | 429–436 ms | 1 | 0 |
| 手机视口 390×844 | 199–203 ms | 429–444 ms | 1 | 0 |

原始数据在 `PERFORMANCE_RESULTS.json`。就绪时间来自 performance.mark；不代表 FCP/LCP、物理手机或真实生产 Blob 延迟，不能作为线上秒开保证。未做截图视觉验收。

## 回归

- 首页登录成功并显示导航；修复旧登录按钮替换内容后品牌节点丢失导致渲染失败的问题。
- 管理页登录成功，日志确认只有 login + navigation 两次请求。
- 模拟管理新增分组与保存成功。
- 503 后显示重新加载按钮，恢复服务后点击可重新读取，非重复使用失败的 Promise。
- 鉴权返回之前不显示私有链接，失效 session 显示登录页。
- 默认无 Google 图标请求；自定义图标保留懒加载。
- 禁用 localStorage 后仍能打开导航并切换主题。
- 320 / 375 / 390 / 768 / 1365 px 视口首页无横向溢出；320 和 375 px 下主题按钮高度 44 px。
- 构建、JavaScript 语法与 diff 空白检查通过。

## 取舍

为保留私有导航边界，不再从 localStorage 在鉴权前展示旧链接，也不在接口故障时展示旧缓存。旧数据不被删除，管理端数据仍由原 Blob 保存。首次内容就绪始终受真实接口响应时间影响；若线上仍慢，下一步应测部署节点与 Blob 响应，不能用公开静态化绕过登录。

## 商务风格与快捷编辑更新

- 深蓝灰主题、系统字体、规整卡片；无新增框架、外部字体或背景图。首页 gzip 约 10.4 KB，仍无外部 JS/CSS。
- 分类添加、右键编辑、手机编辑入口、保存失败保留输入通过本地模拟验证。
- 本地列表就绪 87.6 ms，第一张图标请求 116.4 ms，图标请求在列表显示后发起。
- 手机深色截图已检查，390 px 无横向溢出。桌面浅色截图发现切换瞬间背景过渡不一致，已移除卡片背景动画。
- 图标缓存 5 项测试通过：命中、失败缓存、类型和大小限制、过期回退、主机校验。
- 缓存代码已实现，未部署或修改线上 Blob；云端持久化和真实上游连通性尚待线上验证。

## 菜单、排序与手机简化

桌面分类名称旁的加号、分类排序、分类内网址排序已实现；上下移动后明确保存。右键及卡片省略号打开“打开 / 编辑 / 删除”菜单，删除前确认。排序保存前比对最新 ID 顺序，避免覆盖已变更的列表。

手机首页（650 px 及以下）改为纯浏览：隐藏所有编辑、添加、排序和管理入口，保留主题、搜索、横向分类和双列网址卡片；手机右键/长按不弹出自定义编辑菜单。移动端限制仅为首页交互设计，原管理页和鉴权能力未移除。

本地模拟接口已验证分类与网址排序保存、右键菜单编辑和390 px截图。尚未推送部署。

## 精致视觉调整

重新统一低对比边框、蓝灰色阶、卡片圆角、搜索框和侧栏留白。分类标题旁为相邻的「＋」「↕」图标按钮，排序保留可访问名称和悬浮说明；顶部分类排序也改为图标。手机保持纯浏览。桌面及390 px手机截图已检查，手机无溢出、无编辑按钮。首页gzip约13.6 KB，无外部JS/CSS请求。

## 本次正式推送范围

包含图标缓存、商务视觉、桌面分类旁的添加和排序图标、右键菜单，以及手机44 px紧凑按钮和分类分割线。手机顶部分类栏与编辑入口隐藏。54个示例网站仅由本地预览脚本加载，不写入生产导航数据。构建、5项缓存测试和语法检查通过；线上部署与缓存验证独立于Git推送确认。
